# Copy to .env and fill in. Never commit .env.
GROQ_API_KEY=your_key_here
# Secret used to sign provenance certificates (HMAC). Set a long random value in prod.
PROVENANCE_SECRET=change-me-to-a-long-random-string
